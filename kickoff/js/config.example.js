// config.example.js — Copiez ce fichier en config.js et renseignez votre clé
// Ne jamais commiter config.js (déjà dans .gitignore)

window.ARCEANE_CONFIG = {
  openai_api_key: "sk-VOTRE_CLE_ICI",
  model: "gpt-4o",           // Modèle OpenAI à utiliser
  max_tokens: 1500,
  temperature: 0.2
};
